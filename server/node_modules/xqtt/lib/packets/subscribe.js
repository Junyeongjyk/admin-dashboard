'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _helpers = require('./helpers');

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

exports.default = {
  encode: function encode(packet) {
    var packetType = 8;
    var flags = 2;

    var variableHeader = [packet.id >> 8, packet.id & 0xff];

    var payload = [];

    var _iteratorNormalCompletion = true;
    var _didIteratorError = false;
    var _iteratorError = undefined;

    try {
      for (var _iterator = packet.subscriptions[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
        var sub = _step.value;

        payload.push.apply(payload, _toConsumableArray((0, _helpers.encodeUTF8String)(sub.topic)).concat([sub.qos]));
      }
    } catch (err) {
      _didIteratorError = true;
      _iteratorError = err;
    } finally {
      try {
        if (!_iteratorNormalCompletion && _iterator.return) {
          _iterator.return();
        }
      } finally {
        if (_didIteratorError) {
          throw _iteratorError;
        }
      }
    }

    var fixedHeader = [packetType << 4 | flags].concat(_toConsumableArray((0, _helpers.encodeLength)(variableHeader.length + payload.length)));

    return [].concat(_toConsumableArray(fixedHeader), variableHeader, payload);
  },
  decode: function decode(_buffer, _remainingLength) {
    throw new Error('subscribe.decode is not implemented yet');
  }
};