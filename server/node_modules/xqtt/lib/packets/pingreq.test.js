'use strict';

var _index = require('./index');

describe('packets', function () {
  describe('pingreq', function () {
    describe('encode', function () {
      test('normal', function () {
        expect((0, _index.encode)({
          type: 'pingreq'
        })).toEqual([
        // fixedHeader
        0xc0, // packetType + flags
        0] // remainingLength
        );
      });
    });

    describe('decode', function () {
      test('too short', function () {
        expect((0, _index.decode)(Uint8Array.from([0xc0]))).toBe(null);
      });

      test('normal', function () {
        expect((0, _index.decode)(Uint8Array.from([
        // fixedHeader
        0xc0, // packetType + flags
        0] // remainingLength
        ))).toEqual({
          type: 'pingreq'
        });
      });
    });
  });
});

/* eslint-env jest */