'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = {
  encode: function encode(packet) {
    var packetType = 7;
    var flags = 0;

    return [(packetType << 4) + flags, 2, packet.id >> 8, packet.id & 0xff];
  },
  decode: function decode(buffer, _remainingLength) {
    var id = (buffer[2] << 8) + buffer[3];

    return {
      type: 'pubcomp',
      id: id
    };
  }
};