'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = {
  encode: function encode(_packet) {
    throw new Error('suback.encode is not implemented yet');
  },
  decode: function decode(buffer, remainingLength) {
    var idStart = buffer.length - remainingLength;
    var id = (buffer[idStart] << 8) + buffer[idStart + 1];
    var payloadStart = idStart + 2;
    var returnCodes = [];

    for (var i = payloadStart; i < buffer.length; i++) {
      returnCodes.push(buffer[i]);
    }

    return {
      type: 'suback',
      id: id,
      returnCodes: returnCodes
    };
  }
};