export enum SystemOptionValueType {
    STRING = 'STRING',
    NUMBER = 'NUMBER',
    BOOLEAN = 'BOOLEAN',
    JSON = 'JSON',
}

export enum IpAccessAction {
    ALLOW = 'ALLOW',
    BLOCK = 'BLOCK',
}

export enum BannerTarget {
    ALL = 'ALL',
    USER = 'USER', 
    PARTNER = 'PARTNER',
}

export enum BannerPosition {
    UP = 'UP',
    DOWN = 'DOWN'
}